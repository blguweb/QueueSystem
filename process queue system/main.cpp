#include"head.h"
//缺点：对于指针没有一个统一的格式 导致设计的时候混乱 
int main() {
	//set up time 
	int n = 0;
	
	
	if (click)
	{
		++n;
		get CustomerTime;
		gets CustomerSeviceTime;

	}
	
	//get new
	
	upgrade(window[i], CustomerTime);
	int number = Getshort();
	Push(window[number], CustomerNumber, CustomerTime, CustomerSeviceTime);
	return 0;
}